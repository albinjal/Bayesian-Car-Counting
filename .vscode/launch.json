{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Train Script",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/train.py",
            "args": ["--data-dir", "cowc_pre", "--save-dir", "vgg", "--max-epoch", "5", "--num-workers", "6", "--val-start", "3", "--batch-size", "1", "--val-epoch", "1"],
            "console": "integratedTerminal"
        }
    ]
}
